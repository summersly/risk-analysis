<view class="container">
  <view class="home-container">
    <home-card 
      wx:for="{{calculatorList}}"
      wx:key="key"
      name="{{item.name}}"
      icon="{{item.icon}}"
      route="{{item.route}}"
    >
    </home-card>
  </view>
</view>